This project is called Truck Stops Tracker. It contains the lists of few truck stops I've visited, with their locations and ratings. This is a project I'm developing to practice my React skills. In this project, I have defined a component called Truckstop and passed an object inside the component. The object has three fields:

- name
- location
- ratings
  The objective is to test four examples (objects) that I need to pass through my truck stop component
